# Input the first number from the user
first_number = int(input("Input the first number: "))

# Input the second number from the user
second_number = int(input("Input the second number: "))

# Divide the first number by the second number
result = first_number / second_number

# Display the result
print("The division of the first number and the second number is:", int(result))
